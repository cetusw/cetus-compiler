func Max(arr []int) int {
    max := arr[0]

    for i := 1; i < len(arr); i++ {
        if arr[i] > max {
            max = arr[i]
        }
    }

    return max
}

func main() {
    var nums []int{4, 9, 1, 7, 3}

    println(Max(nums))
}