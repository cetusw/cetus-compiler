# Statements

## Содержание

- [Список операторов](#список-операторов)
- [Простые операторы](#простые-операторы)
- [Присваивание](#присваивание)
- [Printf](#printf)
- [If](#if)
- [Блок](#блок)

## Список операторов

Операторы объединяются в `SequenceASTNode`.

```text
~StmtList~ -> ~StmtList~ ~Stmt~ @sequence
~StmtList~ -> ~Stmt~ @pass_expr
```

## Простые операторы

Простой оператор завершается `;`.

```cetus
a := 2;
printf(a);
a + 1;
```

## Присваивание

```cetus
a := 10;
```

Если имя `a` отсутствует в таблице символов, создаётся новый символ. Если имя уже определено, тип правой части должен совпадать с известным типом.

Присваивание является оператором и имеет тип `void`.

## Printf

```cetus
printf(1);
printf(a > b);
```

`printf` является встроенной конструкцией вывода.

`printf` имеет тип `void`.

> TODO
>
> Заменить `printf` на функцию стандартной библиотеки после спецификации функций и вызовов.

## If

Поддерживаются формы:

```cetus
if a > b {
    printf(1);
}
```

```cetus
if a > b {
    printf(1);
} else {
    printf(0);
}
```

Вложенные `if` разрешены.

`if` является statement-конструкцией и имеет тип `void`. Типы веток `then` и `else` не обязаны совпадать.

## Блок

Блок содержит список операторов:

```cetus
{
    a := 1;
    printf(a);
}
```

Блок имеет тип `void`.

> TODO
>
> Требуется определить области видимости блоков. Сейчас таблица символов фактически плоская.
